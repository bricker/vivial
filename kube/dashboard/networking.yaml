---
apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: dashboard-cert
  namespace: "${NAMESPACE}"
spec:
  domains:
    - "dashboard.${BASE_DOMAIN}"

---
apiVersion: networking.gke.io/v1beta1
kind: FrontendConfig
metadata:
  name: dashboard-fc
  namespace: "${NAMESPACE}"
spec:
  redirectToHttps:
    enabled: true
    responseCodeName: MOVED_PERMANENTLY_DEFAULT

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dashboard-ingress
  namespace: "${NAMESPACE}"
  annotations:
    networking.gke.io/managed-certificates: dashboard-cert
    networking.gke.io/v1beta1.FrontendConfig: dashboard-fc
    kubernetes.io/ingress.global-static-ip-name: dashboard-dot-eave-dot-dev-addr
    kubernetes.io/ingress.class: gce
  labels:
    app: "${APPNAME}"
spec:
  # ingressClassName: gce # This does not work. Without the "ingress.class" annotation, the LB isn't created.
  defaultBackend:
    service:
      name: "${SERVICENAME}"
      port:
        name: http
