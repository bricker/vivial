#!/usr/bin/env bash

set -eu

source ${EAVE_HOME}/develop/python/functions.bash
python-validate-version
python-activate-venv

echo "(lint) isort..."
isort --quiet --check .

echo "(lint) black..."
black --quiet --check .

echo "(lint) mypy..."
mypy .