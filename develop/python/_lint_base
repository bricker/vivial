#!/usr/bin/env bash

set -eu

source "${EAVE_HOME}"/develop/functions.bash

(
	python-lint .
	if ! ^onlythismodule; then
		"${EAVE_HOME}"/libs/eave-stdlib-py/bin/lint
		"${EAVE_HOME}"/libs/eave-pubsub-schemas/python/bin/lint
		"${EAVE_HOME}"/libs/eave-tracing-core-py/bin/lint
		"${EAVE_HOME}"/libs/eave-tracing-python/bin/lint
		"${EAVE_HOME}"/libs/eave-tracing-postgres/bin/lint

	fi
)
