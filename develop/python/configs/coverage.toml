[tool.coverage.run]
data_file = "$EAVE_HOME/.coverage/data"

[tool.coverage.xml]
output = "$EAVE_HOME/.coverage/coverage.xml"

[tool.coverage.html]
output = "$EAVE_HOME/.coverage/coverage_html"

[tool.coverage.annotate]
output = "$EAVE_HOME/.coverage/coverage_annotate"
