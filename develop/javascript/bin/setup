#!/usr/bin/env bash

set -eu

source ${EAVE_HOME}/develop/javascript/functions.bash
node-validate-version
node-activate-venv

rm -rf node_modules

# We have to do this because fish-nvm stores its current version info in a universal variable,
# which is inaccessible to other shells.
usershell=$(shellname)
case $usershell in
	"fish")
		fish -c "npm install --force"
		;;
	*)
		npm install --force
		;;
esac

statusmsg -s "Setup completed ✔"