#!/usr/bin/env bash

set -eu
shopt -s globstar

source "${EAVE_HOME}/develop/functions.bash"

(
	for ss in {apps,libs,develop}/**/bin/lint; do
		if grep -q "node_modules" <<<"$ss"; then
			continue
		fi
		statusmsg -d "Running $ss"
		_ONLY_THIS_MODULE=1 $ss
	done
)
