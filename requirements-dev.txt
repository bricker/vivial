# Requirements that are only installed and used during local development or in CI.

-e ${EAVE_HOME}/libs/eave-pubsub-schemas/python
-e ${EAVE_HOME}/libs/eave-stdlib-py
-e ${EAVE_HOME}/develop/python
-e ${EAVE_HOME}/libs/eave-tracing-core-py
-e ${EAVE_HOME}/libs/eave-tracing-python
-e ${EAVE_HOME}/libs/eave-tracing-postgres

grpcio-tools
protobuf
pyright
ruff
httpx
python-dotenv[cli]
pytest
coverage
pretty_errors
types-requests
types-redis
types-protobuf
mitmproxy==10.1.1
click
alembic
toml