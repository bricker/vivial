{
  "name": "eave-tracing-client",
  "version": "1.0.0",
  "description": "client-side web tracing",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "minify": "uglifyjs dist/eave-client.min.js --compress --mangle --comments '/^\/*!/' --warn --output dist/eave-client.min.js",
    "build": "webpack",
    "prod-build": "webpack --mode production && npm run minify"
  },
  "author": "",
  "license": "GPL-3.0-only",
  "devDependencies": {
    "@babel/preset-env": "^7.24.4",
    "babel-loader": "^9.1.3",
    "express": "^4.19.2",
    "uglify-js": "^3.17.4",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  }
}
