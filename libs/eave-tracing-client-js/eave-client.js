/*

singleton init window.Matomo 

addEventListener
  push it onto dict of listeners per event?
  should we manually handle the listeners like matomo, or rely on the browser to do it? (not sure if we can manually trigger an event if rely on browser)
    create own def of addEventListner to allow for crossbrowser compat

batch atom delivery on a 5s timer?
*/