#!/usr/bin/env bash

set -eu

source "${EAVE_HOME}"/develop/functions.bash

(
	cd "$(^parentpath)"
	"${EAVE_HOME}"/develop/javascript/bin/setup

	cd "$(^parentpath)/typescript"
	"${EAVE_HOME}"/develop/javascript/bin/setup

	cd "$(^parentpath)/python"
	"${EAVE_HOME}"/develop/python/bin/setup
)
