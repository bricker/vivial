#!/usr/bin/env bash

set -eu

source "${EAVE_HOME}"/develop/functions.bash

(
	cd "$(^parentpath)"

	if py-allowed "$@"; then
		python-format .
		python/bin/format
	fi

	if js-allowed "$@"; then
		node-format .
		typescript/bin/format
	fi
)
