#!/usr/bin/env bash

set -eu

source "${EAVE_HOME}"/develop/functions.bash

(
	cd "$(^parentpath)"
	if py-allowed "$@"; then
		python/bin/test
	fi
	if js-allowed "$@"; then
		typescript/bin/test
	fi
)
