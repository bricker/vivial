#!/usr/bin/env bash

set -eu

source "${EAVE_HOME}"/develop/functions.bash

(
	cd "$(e.parentpath)"
	_live_version=$(bin/status | jq -r .version)

	git log --oneline --reverse "$_live_version"..HEAD "$EAVE_HOME"/apps/dashboard "$EAVE_HOME"/libs/eave-stdlib-py
)
