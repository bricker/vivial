#!/usr/bin/env bash

set -eu

function main() (
	local url="www.vivialapp.com"

	case "$(e.gcloudproject)" in
	"eave-staging")
		url="www.eave.dev"
		;;
	esac

	curl -s "https://$url/status" | jq .
)

main "$@"
