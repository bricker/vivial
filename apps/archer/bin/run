#!/usr/bin/env -S bash -eu

function run() (
	local strategy;
	strategy=""

	while getopts "s:" argname; do
		case "$argname" in
		s) strategy=$OPTARG ;;
		*)
			exit 1
			;;
		esac
	done

	PYTHONPATH=. dotenv -f ../../.env run python eave/archer/strategies/"$strategy".py
)

run "$@"