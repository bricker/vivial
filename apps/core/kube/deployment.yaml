apiVersion: apps/v1
kind: Deployment
metadata:
  name: eave-core-deployment
spec:
  selector:
    matchLabels:
      app: eave-core
  template:
    metadata:
      labels:
        app: eave-core
    spec:
      serviceAccountName: sa-eave-core
      containers:
        - name: eave-core-app
          image: eave-core:latest
          resources:
            requests:
              memory: "768Mi"
              cpu: "100m"
            limits:
              memory: "768Mi"
              cpu: "100m"
          ports:
            - containerPort: 80
          env:
            - name: EAVE_DB_HOST
              valueFrom:
                  secretKeyRef:
                    key: EAVE_DB_HOST
            - name: EAVE_DB_USER
              valueFrom:
                  secretKeyRef:
                    key: EAVE_DB_USER

        - name: cloud-sql-proxy
          # https://cloud.google.com/sql/docs/postgres/connect-kubernetes-engine#run_the_in_a_sidecar_pattern
          image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.1.0
          args:
            # If connecting from a VPC-native GKE cluster, you can use the
            # following flag to have the proxy connect over private IP
            - "--private-ip"

            # Enable structured logging with LogEntry format:
            - "--structured-logs"
            - "eavefyi-dev:us-central1:eave-pg-core-dev"

          securityContext:
            # The default Cloud SQL Auth Proxy image runs as the
            # "nonroot" user and group (uid: 65532) by default.
            runAsNonRoot: true
          # You should use resource requests/limits as a best practice to prevent
          # pods from consuming too many resources and affecting the execution of
          # other pods. You should adjust the following values based on what your
          # application needs. For details, see
          # https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
          resources:
            limits:
              memory: "100Mi"
              cpu: "100m"
            requests:
              # The proxy's memory use scales linearly with the number of active
              # connections. Fewer open connections will use less memory. Adjust
              # this value based on your application's requirements.
              memory: "100Mi"
              # The proxy's CPU use scales linearly with the amount of IO between
              # the database and the application. Adjust this value based on your
              # application's requirements.
              cpu:    "100m"