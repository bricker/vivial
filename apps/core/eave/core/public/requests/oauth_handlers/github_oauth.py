            # eave.stdlib.analytics.log_user_action(eave.pubsub_schemas.EaveUserAction(
            #     action_name="eave_application_integration",
            #     description="An integration was added for a team",
            #     eave_account_id=str(eave_account.id),
            #     visitor_id=str(eave_account.visitor_id),
            #     event_source="core api oauth",
            #     opaque_params={
            #         "integration_name": eave.stdlib.core_api.enums.Integration.atlassian.value,
            #         "default_confluence_space_was_used": str(default_space_key is not None),
            #     },
            # ))
