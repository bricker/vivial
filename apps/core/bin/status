#!/usr/bin/env bash

set -eu

function main () (
	local url="api.vivialapp.com"

	case "$(e.gcloudproject)" in
		"eave-staging")
			url="api.eave.dev"
			;;
	esac

	curl -s "https://$url/status" | jq .
)

main "$@"