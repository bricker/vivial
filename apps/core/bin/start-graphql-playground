#!/usr/bin/env bash

set -eu

source "${EAVE_HOME}"/develop/functions.bash

(
	python-activate-venv
	cd "$(e.parentpath)"
	"${EAVE_HOME}/bin/run-with-dotenv" -- strawberry server --host localhost --port 8000 eave.core.graphql.schema:schema
)
