#!/usr/bin/env bash

set -eu

source "${EAVE_HOME}"/develop/functions.bash

function setup-db() (
	run-with-dotenv python eave_alembic/init_database.py
	run-with-dotenv python -m alembic stamp head
)

setup-db