#!/usr/bin/env bash

set -eu

source "${EAVE_HOME}"/develop/functions.bash

statusmsg -w "no tests"
# cd "$(^parentpath)"
# if js-allowed "$@"; then
# 	"${EAVE_HOME}"/develop/javascript/_test_base "$@"
# fi
