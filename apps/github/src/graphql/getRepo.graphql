query ($nodeId: ID!) {
  node(id: $nodeId) {
    ...on Repository {
      id
      name
      url
      description
      createdAt
      updatedAt
      pushedAt

      owner {
        id
        login
        avatarUrl
      }

      defaultBranchRef {
        id
        name
      }
    }
  }
}
