import { CtxArg } from '@eave-fyi/eave-stdlib-ts/src/requests.js';
import Express from 'express';

export async function runApiDocumentation({ req, res, ctx }: CtxArg & { req: Express.Request, res: Express.Response }) {
  // TODO
}