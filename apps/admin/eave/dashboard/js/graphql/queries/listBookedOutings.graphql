query ListBookedOutings($input: AdminListBookingsInput!) {
  adminBookings(input: $input) {
    id
    activityStartTime
    restaurantArrivalTime
    activityName
    restaurantName
    photoUri
    state
  }
}
