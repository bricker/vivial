#!/usr/bin/env bash

set -eu

function main () (
	local url="admin.vivialapp.com"

	case "$(e.gcloudproject)" in
		"eave-staging")
			url="admin.eave.dev"
			;;
	esac

	curl -s "https://$url/status" | jq .
)

main "$@"