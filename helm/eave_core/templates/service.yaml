apiVersion: v1
kind: Service
metadata:
  name: {{ include "eave_core.fullname" . }}
  labels:
    {{- include "eave_core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eave_core.selectorLabels" . | nindent 4 }}
