apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eave_core.fullname" . }}-test-connection"
  labels:
    {{- include "eave_core.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eave_core.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
